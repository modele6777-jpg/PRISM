import React from 'react';

// Zero-overhead component: AuroraBackground already provides cosmic stars and nebulas globally
export function FloatingParticles(_props: { count?: number }) {
  return null;
}

