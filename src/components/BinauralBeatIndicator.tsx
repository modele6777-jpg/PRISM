import React from 'react';

/**
 * BinauralBeatIndicator
 * By user request: separate floating indicator/window is disabled.
 * State is indicated directly via top header emblem and core icons.
 */
export const BinauralBeatIndicator: React.FC = () => {
  return null;
};
